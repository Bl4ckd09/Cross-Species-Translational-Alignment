# Python env & caches
.venv/
__pycache__/
*.pyc
*.pyo

# OS / editor / agent
.DS_Store
.claude/

# Large data — regenerable via scripts/retrieve_expression.py; big-data artifacts belong on HF Datasets
data/_raw/
*.parquet

# Raw third-party expression matrices are NOT redistributed here (see README "Provenance / sources").
# Reproduce them locally with:  python scripts/retrieve_expression.py

# Defensive: never auto-commit large binary/raw formats (e.g. a future TG-GATEs / Plan-A fetch)
*.CEL
*.CEL.gz
*.cel
*.h5
*.hdf5
*.npy
*.npz
*.feather
*.pkl
*.tsv
**/_raw/
